{
    "version": 2,
    "name": "nextjs",
    "builds": [
        { "src": "package.json", "use": "@now/static-build" }
    ],
    "scripts":{
        "now-build": "next build && next export -o dist"
    }
}